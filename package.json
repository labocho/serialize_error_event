{
  "name": "serialize_error_event",
  "version": "1.0.0",
  "description": "",
  "main": "lib/serialize_error_event.js",
  "scripts": {
    "build": "browserify lib/serialize_error_event.js --standalone SerializeErrorEvent -o dist/serialize_error_event.js -t babelify",
    "watch": "watchify lib/serialize_error_event.js --standalone SerializeErrorEvent -o dist/serialize_error_event.js -t babelify -v",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-preset-es2015": "^6.5.0",
    "babelify": "^7.2.0",
    "browserify": "^13.0.0",
    "watchify": "^3.7.0"
  }
}
